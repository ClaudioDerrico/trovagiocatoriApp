<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="trovagiocatoriApp.Views.HomePage"
             Title=""
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F5F7FA">

    <Grid RowDefinitions="Auto, *, Auto, Auto" RowSpacing="0">

        <Grid Grid.Row="0" BackgroundColor="Black" HeightRequest="75">
            <Grid.Shadow>
                <Shadow Brush="#80000000" Offset="0,4" Radius="8" Opacity="0.2"/>
            </Grid.Shadow>
            <HorizontalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                <Image Source="trovagiocatori.png" HeightRequest="55" WidthRequest="220" />
            </HorizontalStackLayout>
        </Grid>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="16" Padding="14,10,14,20">
                <Frame Style="{StaticResource CardStyle}" Margin="0,10,0,10" Padding="0" HeightRequest="240">
                    <Grid>
                        <Image Source="sfondo.png" Aspect="AspectFill" />
                        <BoxView Color="#80000000" />
                        <VerticalStackLayout Spacing="14" Padding="20" VerticalOptions="Center">
                            <Label Text="TROVAGIOCATORI" FontSize="24" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center" />
                            <Label Text="Trova rapidamente compagni per le tue partite" FontSize="16" TextColor="White" Opacity="0.9" HorizontalOptions="Center" HorizontalTextAlignment="Center"/>
                            <Button Text="CREA UN POST" Style="{StaticResource PrimaryButtonStyle}" Margin="0,10,0,0" HorizontalOptions="Center" Clicked="OnCreatePostButtonClicked">
                                <Button.Shadow>
                                    <Shadow Brush="#80000000" Offset="0,4" Radius="4" Opacity="0.3"/>
                                </Button.Shadow>
                            </Button>
                        </VerticalStackLayout>
                    </Grid>
                </Frame>

                <Frame Style="{StaticResource CardStyle}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Seleziona Regione" Style="{StaticResource HeaderStyle}"/>
                        <Border Stroke="#E5E9F0" StrokeThickness="1" StrokeShape="RoundRectangle 12">
                            <Picker x:Name="RegionPicker" Title="Seleziona una regione" ItemsSource="{Binding Regions}" SelectedItem="{Binding SelectedRegion}" HorizontalOptions="FillAndExpand" BackgroundColor="#F8FAFC" FontSize="15" TextColor="#303545" Margin="8,0"/>
                        </Border>
                    </VerticalStackLayout>
                </Frame>

                <Frame Style="{StaticResource CardStyle}" IsVisible="{Binding IsProvincePickerVisible}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="SELEZIONA PROVINCIA" Style="{StaticResource HeaderStyle}"/>
                        <Border Stroke="#E5E9F0" StrokeThickness="1" StrokeShape="RoundRectangle 12">
                            <Picker x:Name="ProvincePicker" Title="Seleziona una provincia" ItemsSource="{Binding Provinces}" SelectedItem="{Binding SelectedProvince}" HorizontalOptions="FillAndExpand" BackgroundColor="#F8FAFC" FontSize="15" TextColor="#303545" Margin="8,0"/>
                        </Border>
                    </VerticalStackLayout>
                </Frame>

                <Frame Style="{StaticResource CardStyle}" IsVisible="{Binding IsSportPickerVisible}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="SELEZIONA SPORT" Style="{StaticResource HeaderStyle}"/>
                        <Border Stroke="#E5E9F0" StrokeThickness="1" StrokeShape="RoundRectangle 12">
                            <Picker x:Name="SportPicker" Title="Seleziona uno sport" ItemsSource="{Binding Sports}" SelectedItem="{Binding SelectedSport}" HorizontalOptions="FillAndExpand" BackgroundColor="#F8FAFC" FontSize="15" TextColor="#303545" Margin="8,0"/>
                        </Border>
                    </VerticalStackLayout>
                </Frame>

                <Button Text="GIOCA ORA" Style="{StaticResource PrimaryButtonStyle}" Margin="20,5" HorizontalOptions="Fill" Clicked="PostPageClicked">
                    <Button.Shadow>
                        <Shadow Brush="#80000000" Offset="0,4" Radius="4" Opacity="0.3"/>
                    </Button.Shadow>
                </Button>

                <Label x:Name="ErrorLabel" IsVisible="False" TextColor="#FF3B30" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center" Text="Compilare tutti i campi" />

            </VerticalStackLayout>
        </ScrollView>

        <!-- Footer con quattro icone: Cos'è, Notifiche, Amici, Profilo -->
        <Grid Grid.Row="2" BackgroundColor="#F8F9FA" HeightRequest="70">
            <Grid.Shadow>
                <Shadow Brush="#000000" Offset="0,-3" Radius="5" Opacity="0.08"/>
            </Grid.Shadow>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Cos'è? -->
            <Frame Grid.Column="0" Style="{StaticResource FooterIconFrameStyle}">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnAboutAppTapped"/>
                </Frame.GestureRecognizers>
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center" VerticalOptions="Center">
                    <Image Source="question_icon.png" Style="{StaticResource FooterIconImageStyle}"/>
                    <Label Text="Cos'è?" Style="{StaticResource FooterIconLabelStyle}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Notifiche -->
            <Frame Grid.Column="1" Style="{StaticResource FooterIconFrameStyle}">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnNotificationsPageTapped"/>
                </Frame.GestureRecognizers>
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center" VerticalOptions="Center">
                    <Grid>
                        <Image Source="notifications_icon.png" Style="{StaticResource FooterIconImageStyle}"/>
                        <!-- Badge notifiche non lette -->
                        <Frame x:Name="NotificationBadge" 
                               IsVisible="False"
                               BackgroundColor="Red" 
                               CornerRadius="10" 
                               Padding="4,2" 
                               HasShadow="False"
                               HorizontalOptions="End"
                               VerticalOptions="Start"
                               Margin="0,-5,-5,0">
                            <Label x:Name="NotificationCountLabel" 
                                   Text="0" 
                                   TextColor="White" 
                                   FontSize="10" 
                                   FontAttributes="Bold"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Frame>
                    </Grid>
                    <Label Text="Notifiche" Style="{StaticResource FooterIconLabelStyle}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Amici -->
            <Frame Grid.Column="2" Style="{StaticResource FooterIconFrameStyle}">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnFriendsPageTapped"/>
                </Frame.GestureRecognizers>
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center" VerticalOptions="Center">
                    <Image Source="friends_icon.png" Style="{StaticResource FooterIconImageStyle}"/>
                    <Label Text="Amici" Style="{StaticResource FooterIconLabelStyle}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Profilo -->
            <Frame Grid.Column="3" Style="{StaticResource FooterIconFrameStyle}">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="ProfilePageTapped"/>
                </Frame.GestureRecognizers>
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center" VerticalOptions="Center">
                    <Image Source="profile_icon.png" Style="{StaticResource FooterIconImageStyle}"/>
                    <Label Text="Profilo" Style="{StaticResource FooterIconLabelStyle}"/>
                </VerticalStackLayout>
            </Frame>
        </Grid>
    </Grid>
</ContentPage>